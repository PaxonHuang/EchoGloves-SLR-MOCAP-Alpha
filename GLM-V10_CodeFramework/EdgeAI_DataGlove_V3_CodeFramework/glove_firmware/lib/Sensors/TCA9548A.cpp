/* =============================================================================
 * EdgeAI Data Glove V3 — TCA9548A I2C Multiplexer Driver (Implementation)
 * =============================================================================
 * NOTE: This file is intentionally minimal because the driver is header-only
 * (defined inline in TCA9548A.h). This .cpp exists to satisfy PlatformIO's
 * library discovery which expects a .cpp file per library subdirectory.
 *
 * All actual logic is in TCA9548A.h. This file simply provides a compilation
 * unit so the header gets included in the build.
 * =============================================================================
 */

#include "TCA9548A.h"

// No additional implementation needed — see TCA9548A.h for full inline class.
// This .cpp placeholder ensures PlatformIO correctly discovers the library.
